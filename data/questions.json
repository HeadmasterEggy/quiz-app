{
  "questions": [
    {
      "id": 1,
      "question": "What is the primary purpose of a session token in web authentication?",
      "options": [
        "To store user passwords securely",
        "To maintain user state across multiple requests",
        "To encrypt database connections",
        "To compress response data"
      ],
      "correct": 1,
      "explanation": "Session tokens are used to maintain user state across multiple HTTP requests. After a user authenticates, the server issues a session token that the client sends with subsequent requests, allowing the server to identify the user without requiring re-authentication."
    }
  ]
}
